<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.solarnetwork.central.dras.dao.mybatis.EventExecutionInfo">
	
	<insert id="insert-EventExecutionInfo" parameterType="EventExecutionInfo">
		<selectKey resultType="long" keyProperty="id" order="BEFORE"> 
              SELECT nextval('solardras.solardras_seq') AS id 
		</selectKey>
		INSERT INTO solardras.program_event_execution 
			(id, evt_id, execution_key, execution_date)
		VALUES
			(#{id}, #{event.id}, #{executionKey}, #{executionDate})
	</insert>

	<update id="update-EventExecutionInfo" parameterType="EventExecutionInfo">
		UPDATE solardras.program_event_execution SET
			execution_key = #{executionKey},
			execution_date = #{executionDate}
		WHERE
			id = #{id}
	</update>

	<resultMap id="EventExecutionInfoFullResult" type="EventExecutionInfo">
		<result column="id" property="id"/>
		<result column="created" property="created"/>
		<result column="evt_id" property="event.id"/>
		<result column="execution_key" property="executionKey"/>
		<result column="execution_date" property="executionDate"/>
	</resultMap>
	
	<select id="get-EventExecutionInfo-for-id" resultMap="EventExecutionInfoFullResult" parameterType="long">
		SELECT id, created, evt_id, execution_key, execution_date
		FROM solardras.program_event_execution
		WHERE id = #{id}
	</select>

</mapper>
